; Picked up automatically by PHP/Nixpacks at the project root.
; Without this, the server's default upload limits can be smaller than what
; Laravel and Nginx are configured to allow (see CLAUDE.md "Upload Rules"),
; causing image uploads to fail before Laravel's validation even runs.
upload_max_filesize = 100M
post_max_size = 150M
memory_limit = 1024M
